{
  "name": "insta-launcher",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "dev:google": "ts-node src/index.ts google",
    "build": "rimraf dist && tsc",
    "build:optimized": "rimraf dist bundle && npx esbuild src/index.ts --bundle --minify --target=es2020 --format=cjs --platform=node --external:playwright --outfile=dist/bundle.js",
    "compile": "npm run build && rimraf out insta-launcher.exe && npx caxa --input . --output out/insta-launcher.exe -- \"{{caxa}}/node_modules/.bin/node\" \"{{caxa}}/dist/index.js\"",
    "compile:optimized": "npm run build:optimized && rimraf out insta-launcher.exe && npx caxa --input . --output out/insta-launcher.exe -- \"{{caxa}}/node_modules/.bin/node\" --max-old-space-size=4096 \"{{caxa}}/dist/bundle.js\""
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "inquirer": "^8.2.7",
    "ora": "^5.4.1",
    "playwright": "^1.30.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.9",
    "@types/node": "^20.10.0",
    "caxa": "^3.0.1",
    "esbuild": "^0.27.3",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
